import { SortByToWatchPipe } from './sort-by-to-watch.pipe';

describe('SortByToWatchPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByToWatchPipe();
    expect(pipe).toBeTruthy();
  });
});
